<h1 align="center">Hi 👋, I'm Kalyan Chary</h1>
<h3 align="center">A passionate Software Engineer from India</h3>

<p align="left"> <img src="https://komarev.com/ghpvc/?username=kalyan1123&label=Profile%20views&color=0e75b6&style=flat" alt="kalyan1123" /> </p>

<p align="left"> <a href="https://github.com/ryo-ma/github-profile-trophy"><img src="https://github-profile-trophy.vercel.app/?username=kalyan1123" alt="kalyan1123" /></a> </p>

<h3 align="left">Connect with me:</h3>
<p align="left">
<a href="https://linkedin.com/in/https://www.linkedin.com/in/kalyan-chary-5502ab273/" target="blank"><img align="center" src="https://raw.githubusercontent.com/rahuldkjain/github-profile-readme-generator/master/src/images/icons/Social/linked-in-alt.svg" alt="https://www.linkedin.com/in/kalyan-chary-5502ab273/" height="30" width="40" /></a>
<a href="https://instagram.com/https://www.instagram.com/kalyan_____________/?utm_source=ig_web_button_share_sheet" target="blank"><img align="center" src="https://raw.githubusercontent.com/rahuldkjain/github-profile-readme-generator/master/src/images/icons/Social/instagram.svg" alt="https://www.instagram.com/kalyan_____________/?utm_source=ig_web_button_share_sheet" height="30" width="40" /></a>
<a href="https://www.leetcode.com/https://leetcode.com/u/rkalyanchary423/" target="blank"><img align="center" src="https://raw.githubusercontent.com/rahuldkjain/github-profile-readme-generator/master/src/images/icons/Social/leet-code.svg" alt="https://leetcode.com/u/rkalyanchary423/" height="30" width="40" /></a>
</p>

<h3 align="left">Languages and Tools:</h3>
<p align="left"> <a href="https://getbootstrap.com" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/bootstrap/bootstrap-plain-wordmark.svg" alt="bootstrap" width="40" height="40"/> </a> <a href="https://www.w3schools.com/css/" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/css3/css3-original-wordmark.svg" alt="css3" width="40" height="40"/> </a> <a href="https://www.docker.com/" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/docker/docker-original-wordmark.svg" alt="docker" width="40" height="40"/> </a> <a href="https://www.w3.org/html/" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/html5/html5-original-wordmark.svg" alt="html5" width="40" height="40"/> </a> <a href="https://www.java.com" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/java/java-original.svg" alt="java" width="40" height="40"/> </a> <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/javascript/javascript-original.svg" alt="javascript" width="40" height="40"/> </a> <a href="https://www.jenkins.io" target="_blank" rel="noreferrer"> <img src="https://www.vectorlogo.zone/logos/jenkins/jenkins-icon.svg" alt="jenkins" width="40" height="40"/> </a> <a href="https://kubernetes.io" target="_blank" rel="noreferrer"> <img src="https://www.vectorlogo.zone/logos/kubernetes/kubernetes-icon.svg" alt="kubernetes" width="40" height="40"/> </a> <a href="https://www.mysql.com/" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/mysql/mysql-original-wordmark.svg" alt="mysql" width="40" height="40"/> </a> <a href="https://www.oracle.com/" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/oracle/oracle-original.svg" alt="oracle" width="40" height="40"/> </a> <a href="https://www.python.org" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/python/python-original.svg" alt="python" width="40" height="40"/> </a> <a href="https://pytorch.org/" target="_blank" rel="noreferrer"> <img src="https://www.vectorlogo.zone/logos/pytorch/pytorch-icon.svg" alt="pytorch" width="40" height="40"/> </a> <a href="https://reactjs.org/" target="_blank" rel="noreferrer"> <img src="https://raw.githubusercontent.com/devicons/devicon/master/icons/react/react-original-wordmark.svg" alt="react" width="40" height="40"/> </a> <a href="https://scikit-learn.org/" target="_blank" rel="noreferrer"> <img src="https://upload.wikimedia.org/wikipedia/commons/0/05/Scikit_learn_logo_small.svg" alt="scikit_learn" width="40" height="40"/> </a> <a href="https://spring.io/" target="_blank" rel="noreferrer"> <img src="https://www.vectorlogo.zone/logos/springio/springio-icon.svg" alt="spring" width="40" height="40"/> </a> </p>

<p><img align="left" src="https://github-readme-stats.vercel.app/api/top-langs?username=kalyan1123&show_icons=true&locale=en&layout=compact" alt="kalyan1123" /></p>

<p>&nbsp;<img align="center" src="https://github-readme-stats.vercel.app/api?username=kalyan1123&show_icons=true&locale=en" alt="kalyan1123" /></p>

<p><img align="center" src="https://github-readme-streak-stats.herokuapp.com/?user=kalyan1123&" alt="kalyan1123" /></p>



..
....

Answer the user's question using ONLY the provided transcript sources. Structure your answer as:
Summary: <one-line overview>
Steps: <numbered steps>

After your answer, on a new line, output exactly:
TranscriptFile: <the source filename without the .txt extension>

If no relevant transcript is found, reply with the single word NOT_FOUND and nothing else.





You are a how-to assistant. Answer the user's question ONLY using the provided training session transcripts.

Format your response in this exact structure:

Summary: one-line description of what the steps achieve.

Steps:
1. ...
2. ...
3. ...

At the very end, on a new line, include:
TranscriptFile: <exact filename without the .txt extension>






Two toggles → repeat error label + border per toggle, add both to submit check.
Say toggles named Toggle1, Toggle2. Swap for real names.
Submit button — OnSelect:

Set(varSubmitted, true);
If(
    Toggle1.Value && Toggle2.Value,
    SubmitForm(Form1),
    false
)

Submits only when both Yes.

Error label under Toggle1:
varSubmitted && Toggle1.Value = false

Error label under Toggle2:
varSubmitted && Toggle2.Value = false

Each toggle own label. Independent. Only the off one turns red.

Border — Toggle1:
If(varSubmitted && Toggle1.Value = false, Color.Red, RGBA(0, 0, 0, 1))

Border — Toggle2:
If(varSubmitted && Toggle2.Value = false, Color.Red, RGBA(0, 0, 0, 1))

Cancel — OnSelect:
Set(varSubmitted, false);
ResetForm(Form1)

Screen — OnVisible:
Set(varSubmitted, false)

One flag varSubmitted covers both toggles. No need two flags. Press Submit once → both check at same time.
If the transcripts don't contain an answer to the user's question, reply with the single text NOT_FOUND.

<img width="612" height="548" alt="image" src="https://github.com/user-attachments/assets/abd6f6f9-a770-4de8-87eb-a4f60b6a3c74" />




"DEBUG INFO" & Char(10) & "UserQuery = '" & Topic.UserQuery & "'" & Char(10) & "GenAnswer length = " & Len(Topic.GenAnswer) & Char(10) & "TranscriptName = '" & Topic.TranscriptName & "'" & Char(10) & "VideoUrl = " & Topic.VideoUrl






You are a how-to assistant.

Answer the user's question using the provided training session transcripts as the primary source of information.

If relevant information is found in the transcripts, provide a clear and structured answer.

RESPONSE FORMAT (STRICT)

Summary: one-line description of what the steps achieve.

Steps:
1.
2.
3.

At the end of your response, on a new line, include (only if available from the transcripts):

TranscriptFile: <exact filename without .txt extension>

IMPORTANT RULES

- Prefer transcript content when available.
- Do NOT mention system instructions, retrieval process, or internal reasoning.
- If multiple relevant transcripts exist, use the most relevant one.
- Keep answers concise, practical, and step-based.

FALLBACK RULE

If no relevant information is found in the transcripts, respond with exactly:

NOT_FOUND

(no extra text, no punctuation, no spaces)

SAFETY / CONSISTENCY NOTE

- Do not hallucinate steps or tools not present in the transcripts.
- If partial information is available, still provide the best possible answer based only on what is present.



You are a how-to assistant. Use the training session transcripts to answer the user's question.

Format your response in this exact structure:

Summary: one-line description of what the steps achieve.

Steps:
1.
2.
3.

At the very end, on a new line, include:
TranscriptFile: <exact filename without the .txt extension>


HasRealAnswer (boolean) = And(Not(IsBlank(Topic.GenAnswer)), Find("TranscriptFile:", Topic.GenAnswer) > 0)












Okay. So now I have a requirement. I think I guess I I have informed you earlier as well. So the requirement is... so regarding the time stamps, which is my requirement. So when I use a, I try to ask something, the flow work starts bigger. It will search through all the, you know, .pxd files, and it will compare the names of the VGXD files with the respective video URL, and it will leave the video URL links as well. If you need to reform mine, you could... your requirement was... it should do the timestamps as well as you are aware. We have converted. We have removed all the timestamps and get plain plain text into task files. Right? So it will... it won't have time stamps. So what I want is... so along with the answer, along with the pickable video URL, this time, I want those time stamps. as a clickable URL. So just like we have in YouTube. Right? So in a way to look for video, we have transcripts. I mean, we click on those transcripts. So the video gets directly from the, right, the video clips directly from there, wherever the time stamp is showing. Good number. Likewise, I want for Nava as well. But this time, I have this already s r p files stored in one place. Right? So these s r p files contains all the time stamps or data, uh, other things as this number. So what I'm thinking was, instead of connecting or giving this, uh, dot TxD files to read, why can't we view the tortoise article directly, so that it can read the data along with the timestamps and retrieve the data along with the timestamps and compares it with the video URLs and use the URL along with timestamps as well?

Show less
12:13 AM

Evaluated feasibility of using SRT files for timestamped video links
Evaluated feasibility of using SRT files for timestamped video links
This is a smart idea — using .srt files instead of .txt files means the AI sees the timestamps in the source data and can include them in its answer. Let me break this down clearly.

Is this possible?
Yes. SRT files are plain text — Copilot Studio can index them as a knowledge source just like your .txt files. The AI will see content like:

1
00:03:40,000 --> 00:03:55,000
Power BI is a business intelligence tool from Microsoft

2
00:03:55,000 --> 00:04:10,000
It allows you to connect to various data sources
And if your prompt tells the AI to include timestamps, it can extract and return them alongside the answer.

Difficulty rating: 5/10
Much easier than I initially estimated for the timestamp feature, because:

.srt files already exist (you confirmed this)
No need to generate timestamps from audio (that was the hard part)
The AI reads the timestamps directly from the source
You just need to format them as clickable links
The approach
Step 1 — Replace .txt source with .srt files
Upload all .srt files to a SharePoint folder (or if they're already there, note the path).
In Copilot Studio → Knowledge → remove the current All Recording's Transcripts (.txt files).
Add the .srt folder as a new knowledge source.
Wait for indexing → Ready.
Step 2 — Update the custom prompt
Tell the AI to include timestamps from the .srt content in its response:

You are a how-to assistant. Answer the user's question ONLY using the provided training session transcripts.

Format your response in this exact structure:

Summary: one-line description of what the steps achieve.

Steps:
1. [HH:MM:SS] Step description
2. [HH:MM:SS] Step description
3. [HH:MM:SS] Step description

Include the timestamp from the transcript for each step so the user knows where in the recording to find it.

At the very end, on a new line, include:
TranscriptFile: <exact filename without the .srt extension>

If the transcripts don't contain an answer to the user's question, your ENTIRE response must be the single word NOT_FOUND - exactly that, all uppercase, with NO punctuation, NO whitespace, NO quotes, NO other text before or after.
Step 3 — Make timestamps clickable
SharePoint video URLs support time-offset parameters. The format is:

https://your-tenant.sharepoint.com/.../video.mp4?web=1&st=220
Where st=220 means start at 220 seconds (which is 03:40).

To turn [00:03:40] into a clickable link, you need to:

Extract the timestamp from the AI's response
Convert HH:MM:SS to total seconds
Append ?web=1&st=<seconds> to the video URL
This conversion could happen in Power Automate (a new Flow 3) or in Power Fx formulas in the topic.

What the final output would look like
From the training session:
Summary: Overview of Power BI, its components, and basic usage.

Steps:
1. [03:40](video_url?st=220) Power BI is a business intelligence tool
2. [07:22](video_url?st=442) Connecting to data sources using Get Data
3. [12:15](video_url?st=735) Creating visualizations in the report view

🎬 Watch full recording: [link]
Each timestamp is a clickable hyperlink that jumps to that exact point in the video. Just like YouTube chapters.

Before we start building — I need to know
Where are the .srt files stored? Give me the SharePoint folder path (like you did for the .txt files).
Do the .srt filenames match the video filenames? For example:
SRT: Introduction to Power BI.srt
Video: Introduction to Power BI.mp4 If yes, Flow 2's matching logic stays the same (just change .txt to .srt in Clean_Name).
How are the videos hosted? Are they played through:
SharePoint's built-in video player?
Microsoft Stream?
Something else? This determines the URL format for time-offset links (?st= vs ?t= vs #t=).
What does one of your .srt files look like? Copy the first 10-15 lines of any .srt file and paste it here — I need to see the exact format so I can write the extraction logic correctly.
My recommendation on sequencing
First: Stabilize the current agent (GenAnswer flakiness) — if the base search doesn't work reliably, adding timestamps on top will be frustrating to debug.
Then: Swap .txt → .srt and add the timestamp feature. Since the architecture stays the same (just a different source + richer prompt + timestamp formatting), it plugs in cleanly.
Answer those 4 questions and I'll map out the exact implementation plan. This is a genuinely good enhancement — .srt files are the perfect shortcut to get timestamps without any audio processing work.

Okay. Let's target about the agent that we are trying to keep. So this time, I'm planning to create a new agent. But this time, we won't keep the power hour for it to, like, be h, k, b, pixel, nothing. Okay? We'll target up on the Excel thing... Excel document. Okay? And this time, I'm only recording to data sources at the first one. Is it dot x r t files? And the second is the video tutorial, the recorded tutorials like, I mean, the actual share for document part. Not part. It's part b a t h. Okay. There we go. Okay. So when in... when I user ask for the agent, the agent knows. So it should search all those for this article. If the answer exists, within some... let's say, I have sixty four SRP files, and my Anthropic is only five SRP files. So the agent should remember the names of the file names. So it should check with the... compare the video URLs to compare the names of the SRP file with the video URL, and then it should give those five video URLs as well. Along with the URLs, I want those time stamps. Wherever the flow identified, the so and so answer has been created. I want those time stamps, So later I have also one more document. The document consists of pushing and something. And I would also like to connect this document once this time stamp thing is completely done and working as expected. Okay. So I inform for that as well. Okay

Show less
12:25 AM

Architected streamlined agent design with multiple video matching and timestamp linking
Architected streamlined agent design with multiple video matching and timestamp linking
Good call starting fresh — a clean agent with a simpler architecture will be much easier to build and debug. Let me confirm what I understood, then we'll plan.

What you want (confirming)
Aspect	Detail
Agent	Brand new, separate from the current one
Data source 1	.srt files (~64 files, stored in SharePoint)
Data source 2	Video recordings (Recorded Tutorials SharePoint library)
No Excel KB	Correct — removed from scope
Core behavior	User asks a question → agent searches .srt files → finds answer across potentially MULTIPLE .srt files → matches each .srt filename to its video URL → returns answer WITH clickable timestamps that jump to the right point in each video
Multiple videos	If the answer comes from 5 .srt files, show ALL 5 video links (not just one)
Future addition	A separate document to connect later (deferred until timestamps work)
Key architecture difference from the old agent
Old agent	New agent
1 matching video	Multiple matching videos
No timestamps	Clickable timestamps
.txt files (clean text)	.srt files (text + timestamps)
Excel KB + transcripts + videos	.srt files + videos only
Complex (3 sources, 2 flows, 11 nodes)	Simpler (2 sources, 1-2 flows)
Before I design the architecture, I need these answers
1. Show me an .srt file sample
Copy-paste the first 20-30 lines of any .srt file. I need to see the exact format:

Is it like this?

1
00:00:01,000 --> 00:00:05,000
Welcome to today's session

2
00:00:05,500 --> 00:00:10,000
Let's start with the basics
Or is it a different format? The exact format determines how the AI reads timestamps and how we extract them.

2. Where are the .srt files stored?
Give me the SharePoint folder path, e.g.:

Documents > Private Documents > Events > Demo > All .srt Files
Or are they in the same folder as the .txt files?

3. Do .srt filenames match video filenames?
For example:

SRT: Introduction to Power BI.srt
Video: Introduction to Power BI.mp4
Same naming pattern as the .txt ↔ .mp4 relationship? Or different?

4. How are the videos played?
When a user clicks a video link in SharePoint, does it open in:

SharePoint's built-in player?
Microsoft Stream?
Something else?
This determines the URL format for time-offset links.

5. One .srt per video, or multiple?
Is it a 1:1 relationship (each .srt file corresponds to exactly one video), or can multiple .srt files map to the same video?

What I'm planning (high-level preview)
User asks: "How do I connect to data in Power BI?"
    │
    ▼
Agent searches .srt files (knowledge source)
    │
    ▼
AI finds relevant content across 3 .srt files:
  - Introduction to Power BI.srt (timestamps 03:40, 07:22)
  - Power BI Basics Day-1.srt (timestamp 15:30)
  - Power BI Data Sources.srt (timestamps 02:10, 08:45)
    │
    ▼
Flow matches each .srt name → video URL + appends timestamp offsets
    │
    ▼
Response:
━━━━━━━━━━━━━━━━━━━━━━━━
Summary: How to connect to data sources in Power BI.

Steps:
1. Open Power BI Desktop and click "Get Data"
2. Select your data source type (Excel, SQL, etc.)
3. Configure the connection and load data

📎 Related recordings:
- Introduction to Power BI
  [03:40](video_url?st=220) — Overview of data connections
  [07:22](video_url?st=442) — Get Data walkthrough

- Power BI Basics Day-1
  [15:30](video_url?st=930) — Live demo of connecting to Excel

- Power BI Data Sources
  [02:10](video_url?st=130) — Supported data source types
  [08:45](video_url?st=525) — DirectQuery vs Import mode
━━━━━━━━━━━━━━━━━━━━━━━━
Each timestamp is clickable — jumps to that exact moment in the video. That's the target.

Answer those 5 questions and we start building
The .srt sample is the most important — everything (prompt design, extraction logic, timestamp formatting) depends on the exact format of your files. Once I see it, I'll design the complete architecture and we build step by step.

also is it possible if we retrieve the data from .srt files can we try to give the retrived the generated solution to generative ai in the copilot studio to refine the answer? and give to the user once refined and enhanced?

12:31 AM

Evaluated two-stage retrieval and refinement pipeline feasibility
Evaluated two-stage retrieval and refinement pipeline feasibility
Yes, absolutely possible — and it's actually the smart way to do it. Here's why.

What you're describing is a two-stage AI pipeline
Stage 1: RETRIEVE
.srt files → raw content with timestamps
(messy subtitle format with timing codes)

         ↓

Stage 2: REFINE
Raw .srt content → Generative AI → polished answer
(clean, structured, human-readable, timestamps preserved)

         ↓

User sees: beautiful formatted answer with clickable timestamps
Why this is a good idea
Raw .srt content looks like this:

1
00:03:40,000 --> 00:03:55,000
so what power bi does is it helps you
connect to different data sources

2
00:03:55,000 --> 00:04:10,000
like excel files sql databases and
cloud services to pull data together
Showing that directly to the user would be ugly and hard to read. But if you feed it to AI with the right prompt, it becomes:

[03:40] Power BI connects to different data sources including 
Excel files, SQL databases, and cloud services to consolidate data.
Clean, readable, timestamps preserved.

Good news — Copilot Studio already does this in one step
The Create generative answers node already does retrieval + generation together. It pulls content from your knowledge source (.srt files), then uses AI to compose an answer. So you don't necessarily need two separate stages — one well-prompted node can do both.

The key is the prompt. You tell the AI:

Read the .srt content (which includes timestamps)
Generate a clean, structured answer
Preserve the relevant timestamps in the output
Format them in a specific way
When you WOULD need two explicit stages
If the single node doesn't preserve timestamps reliably (because its grounding/generation sometimes strips them), then you'd split it:

Stage 1 — Retrieve: Generative Answers node gets raw .srt content, saves it to a variable (minimal prompt, just "return the relevant content as-is with timestamps").

Stage 2 — Refine: Pass that raw content to a second AI step with a refinement prompt ("Take this raw transcript content and rewrite it as a clean, structured answer, preserving all timestamps...").

Stage 2 options in Copilot Studio:

A second Generative Answers node (using the Stage 1 output as input instead of a knowledge source)
A Power Automate flow calling AI Builder's "Create text with GPT" action
A Power Automate flow calling an external AI API (Azure OpenAI, etc.)
My recommendation — start simple, escalate if needed
Approach 1 (try first):     Single node + smart prompt
                            Simplest. Works if prompt is good enough.
                            
Approach 2 (if needed):     Two nodes in sequence
                            More control over timestamp preservation.
                            
Approach 3 (maximum control): Node + Power Automate AI flow
                              Most flexible but most complex.
Start with Approach 1. The prompt would look something like:

You are a training assistant. Answer the user's question 
using ONLY the provided .srt transcript sources.

The source content includes timestamps in [HH:MM:SS] format. 
PRESERVE these timestamps in your answer.

Format your response as:

Summary: one-line description

Steps:
1. [HH:MM:SS] Clear description of this step
2. [HH:MM:SS] Clear description of this step
3. [HH:MM:SS] Clear description of this step

Rewrite the raw subtitle text into clean, professional sentences 
while keeping the original timestamps accurate. Combine fragmented 
subtitle lines into complete thoughts.

At the very end, list ALL source files used:
TranscriptFiles: filename1, filename2, filename3
(without extensions, comma-separated)

If no relevant content is found, reply with NOT_FOUND only.
The AI reads the messy .srt subtitle text, rewrites it into clean sentences, and keeps the timestamps — all in one step.

Summary
Question	Answer
Is it possible?	Yes
Is it a good idea?	Excellent idea — raw .srt is ugly, refined output is professional
Difficulty added?	Minimal if single-node works; moderate if two-stage needed
Should we do it?	Yes — build it into the new agent from the start
Still need from you
Those 5 questions from my previous message — especially the .srt file sample (first 20-30 lines). Once I see the exact format, I'll design the prompt and extraction logic to handle timestamps correctly from the start. The .srt format determines everything.

Send me that sample and we'll start building the new agent.

